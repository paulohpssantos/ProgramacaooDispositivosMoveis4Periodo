<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/unipar_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/unipar_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExemploActivity"
        tools:targetApi="31">
        <activity
            android:name=".CadastroTurmaActivity"
            android:exported="false"/>
        <activity
            android:name=".CadastroDisciplinaActivity"
            android:exported="false" />
        <activity
            android:name=".CadastroProfessorActivity"
            android:exported="false" />
        <activity
            android:name=".CadastroAlunoActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>